= form_for(@client) do |f|
  - if @client.errors.any?
    #error_explanation
      %h2
        = pluralize(@client.errors.count, "error")
        prohibited this information from being saved:
      %ul
        - @client.errors.full_messages.each do |msg|
          %li= msg
  %fieldset
    %legend Enter Account Information
    .field
      = f.label :contact_name
      %br/
      = f.text_field :contact_name
    .field
      = f.label :email_address
      %br/
      = f.text_field :email_address
    .field
      = f.label 'Phone number (optional)'
      %br/
      = f.text_field :phone_number
    .field
      = f.label 'Referral code (optional)'
      %br/
      = f.text_field :referral_code
    .field
      = f.label :userid
      %br/
      = f.text_field :userid
    .field
      = f.label :password, "Password"
      %br/
      = f.password_field :password, :size => 40
    .field
      = f.label :password_confirmation, "Confirm"
      %br/
      = f.password_field :password_confirmation, :size => 40
    .field
      Desired credit balance in $US (optional). You will not be billed now for any entered amount. You will receive an invoice by email for the amount entered in a few business days. 
      Your balance will be reduced by $US10/month per station in your account. See our 
      %a{:href => "/question/index"} FAQs 
      for more invoicing details.
      %br/
      = f.text_field :credit_balance
    .field
      = f.label 'Desired effective date of credit'
      %br/
      = f.date_select :credit_effective_date
    .actions
      / %= f.submit %
      = submit_tag "Submit"
