<%= form_for(@contact_us_email) do |f| %>
  <% if @contact_us_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_us_email.errors.count, "error") %> prohibited this contact_us_email from being saved:</h2>

      <ul>
      <% @contact_us_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<fieldset>
 <legend>Contact Us</legend>

  <div class="field">
    <%= f.label "Your Email Address" %><br />
    <%= f.text_field :from %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.label "Subject" %><br />
    <%= f.text_field :subj %>
  </div>
  <div class="actions">
    <!-- %= f.submit % -->
	<%= submit_tag "Send email" %>
  </div>
  </fieldset>
<% end %>
