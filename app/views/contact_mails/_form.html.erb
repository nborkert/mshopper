<%= form_for(@contact_mail) do |f| %>
  <% if @contact_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_mail.errors.count, "error") %> prohibited this contact_mail from being saved:</h2>

      <ul>
      <% @contact_mail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subj %><br />
    <%= f.text_field :subj %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.label :from %><br />
    <%= f.text_field :from %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
